export {
  spacing,
  borderRadius,
  typography,
  lightColors,
  darkColors,
  lightShadows,
  darkShadows,
  lightTheme,
  darkTheme,
  theme,
} from './theme';
export type { Theme, ColorPalette } from './theme';
export {
  AUTO_SAVE_INTERVAL_MS,
  UNDO_HISTORY_MAX,
  EDITOR_CONTENT_DEBOUNCE_MS,
  STORAGE_KEYS,
  NOTIFICATION_CHANNEL_ID,
  DEFAULT_NOTE_TITLE,
  REMINDER_BODY_MAX_LENGTH,
} from './constants';
